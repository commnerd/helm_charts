apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dhcpd.fullname" . }}-test-connection"
  labels:
    {{- include "dhcpd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dhcpd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
